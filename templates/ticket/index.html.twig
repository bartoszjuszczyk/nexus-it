{% extends 'base.html.twig' %}

{% block title %}{{ page_title }}{% endblock %}

{% block content %}
    <main class="app-main-content">
        <div class="container">
            <div class="ticket-list">
                {% for ticket in tickets %}
                    <article class="ticket-card">
                        <div class="ticket-header">
                            <h3 class="ticket-title">
                                <a href="{{ path('app_ticket_view', {id: ticket.id}) }}">{{ ticket.title }}</a>
                            </h3>
                            <span class="status-badge status-resolved">Rozwiązane</span>
                        </div>
                        <div class="ticket-body">
                            <p>{{ ticket.description }}</p>
                        </div>
                        <div class="ticket-footer">
                            <span class="ticket-meta">ID: #{{ ticket.id }}</span>
                            <span class="ticket-meta">Data: {{ ticket.createdAt|date('d.m.Y') }}</span>
                            <a href="{{ path('app_ticket_view', {id: ticket.id}) }}" class="btn btn-secondary btn-sm">
                                Show details
                            </a>
                        </div>
                    </article>
                {% endfor %}
            </div>
        </div>
    </main>
{% endblock %}
